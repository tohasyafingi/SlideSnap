# SlideSnap Environment Variables

# Server port (default: 3000)
PORT=3000

# Node environment (development | production)
NODE_ENV=production

# API CORS origin (optional, defaults to *)
CORS_ORIGIN=https://slidesnap.example.com
